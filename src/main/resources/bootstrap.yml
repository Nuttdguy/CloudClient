 # .yml Client Bootstrap file
---
spring:
  profiles:
    active: default
  application:
    name: cloud-server
  cloud:
    config:
      uri: http://localhost:8001

server:
  port: 8002

#spring.cloud.config.uri = http://localhost:8001
#server.port = 8002